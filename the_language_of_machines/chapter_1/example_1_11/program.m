ascii;
control 0 : Accept 1;
input;
stack empty string;

0 to 0;         scan (;         push (;
0 to 0;         scan );         pop (;
0 to 0;         scan [;         push [;
0 to 0;         scan ];         pop [;
0 to 1;         eof;            empty;
