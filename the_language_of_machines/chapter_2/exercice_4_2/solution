A control that has as repertory every partial function from Q to Q can be transformed to a
conventional control. For each instruction of the form (f, i₁, ...) the conventional control has the
set { (a to b, i₁, ...) | (a, b) ∈ f }.
